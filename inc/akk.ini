[database]
driver = mysql
host = localhost
;port = 3306
db = akk
username = dbuser
password = dbpwd

[akk]
Veranstaltung = "LPT 15.2"
Datum = "24.10. - 25.10.2015"
Ort = "Aachen (TEST)"
Ebene = LV

[system]
#
# rootdir fuer die Scripte zum DB Upload und htpasswd
# falls nichts angegeben ist /web/akk auch der default
# rootdir = /web/akk

# htpasswd kann man setzen - wenn nicht gesetzt wird
# der folgende Default genommen:
# htpasswd = $rootdir/passwd.users
# htpasswd = /web/akk/data/passwd.users
